# ============================================================
# 本地 PostgreSQL 容器的密码配置模板
# ============================================================
# 用法：把本文件复制一份、改名为 .env（去掉 .example），再填上你自己的密码。
#
#     Windows PowerShell:  Copy-Item .env.example .env
#     macOS / Linux:       cp .env.example .env
#
# 说明：这个密码只用于【本地开发】的数据库容器。
#       容器端口只绑定在 127.0.0.1（本机），外网连不进来，所以设个自己记得住的即可。
#
# ⚠️ 两处密码的分工（必须一致，否则报 authentication failed）：
#      - 这里：容器第一次创建时，给 postgres 用户「设」密码（一次性）
#      - backend/.env 的 DB_PASSWORD：Django 每次连库时「报」密码（每次都读）
#
# ⚠️ 注意：这个变量只在【容器第一次创建、数据目录还是空的时候】有用。
#    如果容器已经跑过（卷里有数据），改这一行【不会】改掉已有密码 —— 它已经写进数据目录了。
#    要真正修改，需进数据库执行：
#        docker exec -it aifriends-pg psql -U postgres -c "ALTER USER postgres WITH PASSWORD '新密码';"
#    再同步改 backend/.env 里的 DB_PASSWORD。

POSTGRES_PASSWORD=change_me_to_your_own_password
